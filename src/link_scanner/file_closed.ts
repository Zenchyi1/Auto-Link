import { scanner } from "./scanner";

class File_closed extends scanner {
    public setup(plugin: any) {

    }
    public search(plugin: any): void {

    }
}
export const file_closed = new File_closed();